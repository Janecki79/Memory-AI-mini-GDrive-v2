{
  "name": "memory-ai-mini",
  "version": "1.0.0",
  "private": true,
  "description": "Minimalny backend Memory AI + integracja Google Drive",
  "engines": {
    "node": ">=20 <23"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "echo \"(brak testów)\" && exit 0",
    "smoke-test": "node server.js & pid=$!; sleep 2; curl -fs http://localhost:${PORT:-3000}/status && kill $pid"
  },
  "dependencies": {
    "express": "^4.19.2",
    "multer": "^1.4.5-lts.1",
    "dotenv": "^16.4.5",
    "googleapis": "^126.0.1",
    "fs-extra": "^11.2.0",
    "cors": "^2.8.5"
  }
}
